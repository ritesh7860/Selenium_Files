import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class Company_Details {

	public static void main(String[] args) 
	{
		ChromeDriver driver = new ChromeDriver();
		driver.get("https://clutch.co/us/developers/information-technology-industry");
		driver.manage().window().maximize();
		
		for(int i=1;i<=65;i++) {
			 String xpath = String.format("(//div/div[1]/h3/a)[%d]", i);

             // Wait for the element to be present and visible
             WebElement element = driver.findElement(By.xpath(xpath));

             // Perform actions with the element
             System.out.println("Element " + i + ": " + element.getText()); // Print the text of the element
             // You can also click the element if needed
             // element.click();		
		}
	}

}
